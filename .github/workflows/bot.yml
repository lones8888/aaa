name: Run Strategy Bot

on:
  schedule:
    - cron: "0 0,4,8,12,16,20 * * *"   # UTC saatleri → Türkiye’de 03:00, 07:00, 11:00, 15:00, 19:00, 23:00
  workflow_dispatch:   # Manuel tetikleme de kalsın

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set timezone to Europe/Istanbul
        uses: zcong1993/setup-timezone@master
        with:
          timezone: Europe/Istanbul

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests pandas

      - name: Run strategy
        run: python strateji.py
